# Images

Static images served from `/images/...`.

Expected files:

- `dimitry-b-tRGqh8cHanA-unsplash.jpg` — homepage background (orangutan on a
  branch). Photo by Dimitry B on Unsplash. Credited on the /apresiasi page.
  Copy the file here; until it exists the homepage simply shows the background
  colour (no broken image).
