# NestJS API base (Railway), no trailing slash
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# Cloudflare R2 public base serving the PMTiles, no trailing slash
NEXT_PUBLIC_TILES_BASE_URL=https://tiles.example.com
